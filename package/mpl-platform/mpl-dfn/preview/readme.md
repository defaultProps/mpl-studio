系统组件，直接生成pageVueCode预览。
业务模板：在线Vue挂载渲染[可手动切换为源码引用预览]
自定义组件模板：只能使用源码引用预览。
源码引用时，必须走构建部署流程。

-------

源码引用固定文件夹。固定推送到指定文件夹路径，`nodejs file.require("@/mpl/bussModules/*")`

----------

细节：业务模板与pageVueCode主页面的数据交互传递formData、事件传递、表单校验、前端API调用等。
主题样式通用。